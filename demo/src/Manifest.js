'use strict';

let dataDir = '../data';

export default {
  images: [{
    name: 'img',
    path: `${dataDir}/test.png`
  }],

  atlases: [{
    name: 'pac',
    imgPath: `${dataDir}/atlas/pac.png`,
    metaPath: `${dataDir}/atlas/pac.json`
  }]
};